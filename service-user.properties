spring.application.name=serviceuser
server.port=8081
# ===============================
# Datasource Config
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/userdb
spring.datasource.username=postgres
spring.datasource.password=N

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.data.jdbc.dialect=postgresql
# ===============================
# Flyway (DB migrations)
Recommandation gÃ©nÃ©rale
# â¢ En production, mettez plutÃ´t `ddl-auto=none` et laissez Flyway gÃ©rer TOUTES vos
Ã©volutions de schÃ©ma.
# â¢ En prÃ©prod ou test, vous pouvez garder `ddl-auto=validate` pour Ãªtre sÃ»rs quâaucune
entitÃ© nâest hors-circuit.
# ===============================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
# ===============================
# Actuator (Monitoring)
# ===============================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# ===============================
# Config Eureka Client
# ===============================
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/